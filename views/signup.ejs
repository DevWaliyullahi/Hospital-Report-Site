<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Doctor Signup</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 20px;
      background: linear-gradient(to right, gold, black);
      color: #fff; /* Adjusted font color for better contrast */
    }

    h1 {
      color: gold;
      text-align: center; /* Center-align the heading */
    }

    form {
      max-width: 400px;
      margin: 20px auto;
      padding: 20px;
      border: 1px solid #ccc;
      border-radius: 5px;
      background: linear-gradient(to right, black, gold); /* Updated background color */
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    label {
      display: block;
      margin-top: 10px;
      color: gold; /* Label text color */
    }

    input,
    select {
      width: 100%;
      padding: 8px;
      margin-top: 5px;
      margin-bottom: 10px;
      box-sizing: border-box;
      background-color: #f8f8f8; /* Lighter background color for input fields */
      color: #000; /* Black text color for input fields */
    }

    button {
      background-color: gold;
      color: #000; /* Adjusted text color */
      padding: 10px 15px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    button:hover {
      background-color: black;
      color: gold; /* Adjusted text color on hover */
    }
  </style>
</head>

<body>
  <h1>Doctor Signup</h1>

  <form action="/doctors/signup" method="post">
    <label for="doctorsName">Doctor's Name:</label>
    <input type="text" name="doctorsName" id="doctorsName" required>

    <label for="email">Email:</label>
    <input type="email" name="email" id="email" required>

    <label for="password">Password:</label>
    <input type="password" name="password" id="password" required>

    <label for="specializations">Specialization:</label>
    <input type="text" name="specializations" id="specializations" required>

    <label for="gender">Gender:</label>
    <select name="gender" id="gender" required>
      <option value="male">Male</option>
      <option value="female">Female</option>
      <option value="other">Other</option>
    </select>

    <label for="phoneNumber">Phone Number:</label>
    <input type="tel" name="phoneNumber" id="phoneNumber" required>

    <button type="submit">Submit</button>
  </form>
</body>

</html>
